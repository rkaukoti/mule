<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:vm="http://www.mulesoft.org/schema/mule/transport/vm"
      xmlns:transports="http://www.mulesoft.org/schema/mule/transports"
      xmlns:test="http://www.mulesoft.org/schema/mule/test"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/test http://www.mulesoft.org/schema/mule/test/current/mule-test.xsd

        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/transport/vm http://www.mulesoft.org/schema/mule/transport/vm/current/mule-transport-vm.xsd
        http://www.mulesoft.org/schema/mule/transports http://www.mulesoft.org/schema/mule/transports/current/mule-transports.xsd">

    <transports:endpoint name="out" exchange-pattern="request-response" address="${outbound.destination}"/>

    <vm:connector name="vmConnector"/>

    <flow name="JmsQueueComponent">
        <transports:inbound-endpoint address="vm://in" exchange-pattern="request-response"/>
        <transports:outbound-endpoint ref="out"/>
    </flow>

    <flow name="EchoComponent">
        <transports:inbound-endpoint ref="out"/>
        <test:component appendString=" Received"/>
    </flow>

</mule>
